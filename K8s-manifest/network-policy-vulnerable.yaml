apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vulnerable-network-policy
  namespace: chat-app
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - {}
  egress:
  - {}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: misconfigured-blocking-policy
  namespace: chat-app
spec:
  podSelector:
    matchLabels:
      app: back-selec
  policyTypes:
  - Ingress
  - Egress


